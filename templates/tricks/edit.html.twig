{% extends 'base.html.twig' %}

{% block title %}
    Modification de {{ tricks }}
{% endblock %}

{% block body %}
    <div>
        <h1>{{ block('title') }}</h1>
        {{ include('tricks/_form.html.twig', { 'button_label': 'Edit'}) }}
    </div>
{% endblock %}

